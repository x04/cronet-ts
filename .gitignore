# Dependencies
node_modules/

# Build output
dist/
target/

# Environment
.env
.env.*
!.env.example

# Chromium source (fetched at build time)
chromium/
depot_tools/

# Native addon binaries (dev builds in cronet-node)
packages/cronet-node/*.node
packages/cronet-node/*.dylib
packages/cronet-node/*.so
packages/cronet-node/icudtl.dat

# Cronet shared libraries (build intermediates)
packages/cronet-sys/lib/

# ICU data (root copy)
/icudtl.dat

# OS
.DS_Store
